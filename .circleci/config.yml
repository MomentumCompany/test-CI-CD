version: 2.1
jobs:
    build:
        docker:
            - image: circleci/node:7
        steps:
            - checkout
            - run:
                name: install-dependencies
                command: npm install
            - deploy:
                name: digital-ocean
                command: ssh -o "StrictHostKeyChecking no" $DROPLET_USER@$DROPLET_IP "cd /var/www/html/lostAndFound_Backend; git pull; npm install; npm run build;" 
                

      
